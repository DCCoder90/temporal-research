module temporal-signals

go 1.22

require go.temporal.io/sdk v1.26.1

// go.sum is generated on first `go mod tidy` or `docker compose build`.
// To develop locally: run `go mod tidy` in this directory once to populate go.sum.
